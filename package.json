{
  "name": "warehouse-quality-integration",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply warehouse_quality_db --local",
    "db:migrate:remote": "wrangler d1 migrations apply warehouse_quality_db --remote",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "xlsx": "^0.18.5"
  }
}
