{
  "name": "warehouse-quality-integration",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "for f in migrations/*.sql; do wrangler d1 execute warehouse-quality-db --local --file=\"$f\"; done",
    "db:migrate:remote": "for f in migrations/*.sql; do wrangler d1 execute warehouse-quality-db --remote --file=\"$f\"; done",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^2.0.0",
    "pdf-lib": "^1.17.1",
    "xlsx": "^0.18.5"
  }
}
